#include <stdio.h>
#include "main.h"
/**
 * _abs- compute absolute value of an integer
 * @n: int to be checked out
 * Return: an absolute integer
 */
int _abs(int n)
{
	if (n >= 0)

	{
	return (n);
	}
	return (-n);
}
